import { StyleSheet } from 'react-native'

export const commonStyles = StyleSheet.create({
    h1text: {
      fontSize: 30,
      fontWeight: 'bold',
      padding: 20,
      marginLeft: "auto",
      marginRight: "auto",
    },
})